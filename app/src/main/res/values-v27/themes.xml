<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="LockScreenActivity" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:background">@color/black</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:windowLayoutInDisplayCutoutMode">
            shortEdges <!-- default, shortEdges, never -->
        </item>
    </style>

    <!-- ====== -->

    <style name="PreferencesActivity" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/purple_700</item>
        <item name="android:background">@color/preferencesBackgroundColor</item>
        <item name="android:textColor">@color/preferencesTextColor</item>
        <item name="android:windowLayoutInDisplayCutoutMode">
            shortEdges <!-- default, shortEdges, never -->
        </item>
    </style>
</resources>
